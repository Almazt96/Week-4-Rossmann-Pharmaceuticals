import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
from sklearn.preprocessing import StandardScaler
from scipy.stats import zscore
import logging

# Initialize Logging: For traceability.

logging.basicConfig(filename='eda_log.log', level=logging.INFO,
                    format='%(asctime)s - %(levelname)s - %(message)s')
logging.info("EDA process initiated.")

# Load and Explore Data
# Data Loading
# Load datasets (train.csv, test.csv, etc.) into Pandas dataframes.

train = pd.read_csv("D:/Personal/Kifiya 10 Academy/Week 4/data/Data/rossmann-store-sales/train.csv")
test = pd.read_csv("D:/Personal/Kifiya 10 Academy/Week 4/data/Data/rossmann-store-sales/test.csv")

# Print shape and a quick preview of data.

logging.info(f"Training data shape: {train.shape}")
train.info()
train.head()

# 2.	Understand Columns
# Assess dtype, missing values, and summary statistics.

train.describe()
train.isnull().sum()
